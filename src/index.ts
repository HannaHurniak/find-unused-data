import { formatDate } from "utils/format_data";
import { FUNCTIONS_NAME } from "func";
import { DAY } from "./constants/constants";

const day = new Date();
console.log(FUNCTIONS_NAME);
formatDate(day);
